# Handoff — P12A S3b: steps B, C, D (W5/W6, W7, W9/W10/W11) + phase close-out

**This file is the complete brief.** Dan starts the session with nothing but
`continue from handoffs/2026-09-16-graph-p12a-s3b.md` — there is no additional prompt
coming, and no context in the chat above. Read this file in full before acting, follow
the read-order below, and treat anything not written here as something you must discover
from the repo rather than ask for.

Working directory: `/home/dan/projects/maestro`.

---

## Goal

Finish Phase P12A. S1/S1b closed W0 and W12. S2 closed W1, W2, W3 and W8, plus S-10/S-11
(`b738faa`, `4ec3a69`, `0bc0e97`, `ea82b01`). **S3 has started.** The previous session
wrote a **fixed S3 design contract** and **step A (W4 candidate reap) is done and
independently verified as `67ed72b`**. What remains is **steps B, C and D** of that
contract, then closing the six threads and the phase close-out.

**Read first, in this order:**
1. **`handoffs/2026-09-16-graph-p12a-s3-design.md` — the S3 design contract. It is your
   spec for steps B (W5/W6), C (W7) and D (W9/W10/W11).** Its decisions are fixed.
2. `docs/graph-engineering/specs/phases/P12A_GRAPH_DISPATCH_WIRING.md` §2 W5/W6/W7/W9/W10/W11, §3, §4.
3. `grep -n 'in_progress_details' -A40 docs/graph-engineering/STATE.yaml` (step A's record
   and its known limit are there).
4. `handoffs/2026-09-15-graph-p12a-s2-design.md` — S2's contract, for the shape S3 builds on.

**Standing rule from Dan:** the Context Gate is **NOT built** and gets built
independently. Implement none of it. §4 still binds: **one dispatch choke point**
(`WorkflowRunner.launch_worker`), **one fresh-vs-resume decision**
(`routing.session_decision`), **no second context rotation**.

## Step A as delivered (`67ed72b`) — build on it

- `compiler.compose`: `_Group.reaped_by_join` (fan_out `candidates`, width >= 2) -> required
  `always` edges into the join, no per-candidate `on_failure -> park`. Width 1 byte-identical.
- `runner.py`: `CANDIDATE_SELECT_HANDLER`, `WorkflowRunner.candidate_documents(node)`;
  `node_inputs` sets `inputs["candidates"]` last for the join.
- Tests: `test_a_crashed_candidate_is_reaped_as_abandoned_and_one_winner_is_selected` and
  `test_only_a_committed_result_becomes_a_candidate_document` in
  `tests/graph_engineering/test_dispatch_wiring.py`; compiler edge tests in
  `tests/workflows/test_candidates.py`.
- **Deviation / known limit (put it in the W4 thread's note when closing it):** a candidate
  whose writer dies with no receipt is reconciled to attempt `uncertain` (the
  `agent_editing_worktree` effect is not repeatable, A05) and its node stays `running`, so
  the join waits. The lane test crashes c3 through a *failed launch* (the `_fail_claim`
  path) instead. Letting a dead-writer candidate reach the join needs a
  reconcile -> node-phase transition that also changes `routing is None` behaviour; it was
  not built. Close W4 as `closed`, mention this limit in the note, and raise it with Dan in
  the closing report as a decision for him.
- Verified by the orchestrator after the commit: `tests/graph_engineering tests/workflows
  tests/backends` junit **778/0**, exit 0. Agent-reported: gate command 299/0,
  dispatch_wiring 42/0. The full suite was **not** run after step A.

## How to run B, C, D (same pattern as S2 and S3 step A)

One background `general-purpose` subagent per step, **sequentially** (they share
`runner.py`, `orchestrator.py`, `test_dispatch_wiring.py`). The step-A prompt that worked
told the agent to: read the design contract (only its own step + "Every step ends with"),
this file, and the P12A spec sections; treat decisions as fixed and stop-and-report on
impossibility or §4 drift; use TDD; put scratch/junit under `.scratch/s3/`; read junit root
`testsuite` counts; make no STATE / AGENTS.md / GEMINI.md edits; never run privileged
commands; make one commit ending with the Co-Authored-By trailer; reply with a <=25-line
report (commit hash, junit counts, deviations). After each step, **personally**
`git show --stat HEAD` and re-run `tests/graph_engineering tests/workflows tests/backends`
(plus the step's other named test files) before starting the next. Record each step in
STATE `in_progress_details.what_was_done` (targeted replacement, like step A's lines).

**Dan decided two things after step A — read them at the end of the design contract (D-FAIL-1, D-FAIL-2).** In short: `engineering.max_plan_repairs` defaults to **1**; the crash/fallback/escalation work is a new phase after P12A, so do not build it in S3; and in the P12A close-out, name that new phase as the next work before P12 in STATE.

Hook trap: a PreToolUse hook rejects any Bash command whose text contains the privileged
word (s-u-d-o), even inside a string. Put such text in a file via Write, not in a heredoc.

## What S2 built (build on it; don't redo it)

- **`maestro/workflows/routing.py`** — `DispatchRouting` (catalog, policy, queries,
  task_spec, statistics, resources, `usage_reader`, `admit`, `on_attempt_finished`),
  `build_manifest`, `resolve`, `deadline_sec`, `plan_for`, `runtime_kind`,
  `render_binding_argv`, `session_decision`, plus W3's `usage_to_dict` / `read_usage` /
  `reset_crossing` / `load_statistics` / `record_outcome` / `binding_view` /
  `shape_key_from_json`.
- **`runner.advance`** is `tick`'s only per-frontier-node call: admit → manifest → one
  `resolve` → `claim(deadline_sec=)` → record → `dispatch`. A `wait` adds to
  `report.waiting` and claims nothing; a `block` or manifest refusal goes through
  `_refuse` → `_fail_claim`, the single failure path shared with launch failure.
- **W3 durable decision:** 15 nullable `attempts` columns via `ControlStore._ADDED_COLUMNS`,
  the append-only `routing_outcomes` table (INSERT only; a later differing verdict appends
  with `amends=<previous outcome_id>`), and `lifecycle.record_routing_decision` /
  `record_routing_settle` / `attempt_claimed_at`. Attempts rows are still written **only**
  through `control/lifecycle.py`. `status.explain_view` carries a `routing` block per
  attempt; `render_explain_text` prints the binding identity. The `effort` column stores
  `effort_native_value or "unsupported"`, provably the same value as `ShapeKey.effort`.
- **W8 host admission:** `orchestrator._GraphHostAdmission` — `begin(store, now)` sweeps
  dead holds and builds one `_admission_round` per poll; `admit` does
  `ResourceRequest.from_node` → `round.admit` → `_reserve_host_resources`, withdrawing on
  refusal; `on_attempt_finished` releases (called from `_commit_result` and `_fail_claim`).
  The three legacy helpers are **reused, not copied**. A hold is keyed by the *prospective*
  attempt id, so `counted_live` is genuinely `attempt_id -> amounts`. Nodes with no counted
  host amounts skip the round and the ledger entirely, so the stock graph is untouched.
- **S-10:** `ModelProfile.usage_pool_id`, `BackendCapabilityEntry.pool_for(model_id)`, agy's
  `claude-*` / `gpt-oss-*` on `AGY_THIRD_PARTY_POOL = "antigravity-3p"`, `Usage.pools`.
  `router.resolve_agent` computes `pool_id = entry.pool_for(...)` once and uses it for both
  admission and `binding.usage_pool_id`.
- **S-11:** agy's `--sandbox` and headless permission mode were exercised on 2026-09-16
  (evidence: `.scratch/s2b/s11-agy-sandbox-observation.md`).

## Two invariants S2 established that S3 must not break

1. **`Usage.pools` is deliberately absent from `to_usage_json` / `from_usage_json`.** That
   document is the shared contract between the drivers, the statusline sampler and
   `quota.py`; it already omits `session_id` and `token_split`. A test pins this. Adding
   `pools` would desynchronise the sampler.
2. **`Capabilities.sandbox` stays `False` for agy.** Under `--sandbox` agy still wrote to
   the workspace — it restricts the *terminal*, not the filesystem, and `base.py:96`
   defines that flag as "can constrain filesystem/network access". Only its stated reason
   changed. `--mode accept-edits` remains uncharacterised and is recorded as an honest gap:
   the S2 session's third probe was refused by its own permission classifier, and that
   refusal was deliberately not worked around. If you need that path characterised, expect
   the same refusal and raise it with Dan rather than routing around it.

   The S-11 evidence, including the exact argv of both probes and the quota readings taken
   before they ran, is in `.scratch/s2b/s11-agy-sandbox-observation.md`. S2's verification
   junit files are in `.scratch/s2b/` (git-ignored, may have been swept).

## The six threads S3 owns (still `owner: P12A`)

- `_p07_open_threads` #2 — candidate_select reap = **W4**
- `_p10_open_threads` #1 — select_affected_checks / plan_repair / apply_repair = **W5 / W7**
- `_p10_open_threads` #2 — shadow comparisons not persisted = **W6**
- `_p10b_open_threads` #4 — host capacity park = **W9**
- `_p10b_open_threads` #5 — async hold re-derived each poll = **W10**
- `_p11_open_threads` #3 — nothing lists a task's runs (`_select_run`) = **W11**

Closing one means **dropping `owner:` AND adding `disposition:` + `note:`**.
`scripts/next_graph_prompt.py:344 unstructured_threads` fails a thread carrying *both* or
*neither*, and `tests/test_next_graph_prompt.py:75` enforces it against the live file.
`THREAD_DISPOSITIONS = ("accepted", "closed", "needs-dan")`. Count threads with
`iter_open_threads`, not `grep -c 'owner: P12A'` — the raw grep also matches note text.

## How S2 was run (this pattern worked)

An S2 design contract was written first, then one background subagent per step (A–D), run
**sequentially** because the steps share `runner.py`, `orchestrator.py` and
`tests/graph_engineering/test_dispatch_wiring.py`. The orchestrating session personally
re-ran each step's suites and inspected each commit rather than trusting the report.
Writing an S3 design contract first is recommended — it is what kept four steps coherent.

## Coordination

- Branch `feat/graph-engineering-foundation`, **no upstream** (`git pull` fails).
  `git log --oneline -3` should show `67ed72b` (S3 step A), `10e5482`, `ea82b01`.
- `STATE.yaml` and `handoffs/` are **git-ignored** — edit STATE, but it never appears in a
  commit. Targeted replacements only; `in_progress_details` is the live slot.
- `AGENTS.md` and `GEMINI.md` are sibling agents' own instruction files; they are now
  git-ignored (`.gitignore:29-30`, added 2026-09-16) and are not maestro's to version.
  `.scratch/` is still untracked and will keep showing in `git status` — don't commit it.
- Leave `docs/graph-engineering/specs/phases/P12A_*` alone. Don't flip the `legacy` default.
- Put work files in `.scratch/`, **not `/tmp`** (the session scratchpad is cleared mid-run).
- Commit messages end with `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.

## In scope / Out of scope

- **In:** steps B/C/D (W5/W6, W7, W9, W10, W11); closing all six threads (incl. W4's); the P12A close-out
  (`status` / `current_phase` in STATE.yaml).
- **Out:** any Context Gate piece; flipping the `legacy` default; P12.

## Verification (report the numbers)

```bash
.venv/bin/python -m pytest -q tests/graph_engineering/test_dispatch_wiring.py --junit-xml=.scratch/s3/s2.xml
.venv/bin/python -m pytest -q tests/graph_engineering/test_dispatch_wiring.py tests/test_host_resource_admission.py tests/test_workflow_status.py tests/workflows --junit-xml=.scratch/s3/gate.xml
.venv/bin/python -m pytest -q --junit-xml=.scratch/s3/full.xml   # must exit 0
```

Read `tests` / `failures` off the **root `testsuite`** element — pytest's summary line is
suppressed in this repo.

**Verified baselines at S2 close (2026-09-16):** s2 `40/0` · gate `295/0` ·
backends+graph+next_graph_prompt `571/0` · **full suite `4531/0 skipped=1`, exit 0**.

Report per §3: **W4** (done in step A; re-confirm green) — a 3-candidate free-quality run with one crashed candidate selects
exactly one winner and reports the crashed one `abandoned` (never dropped). **W5/W6** — a
shadow-mode verification node runs the full suite and one `ShadowComparison` row survives a
controller restart; switching to `authoritative` below `minimum_runs` is refused with the
count still missing. **W7** — a fixture diagnoser proposal moves the loop to revision N+1
under a new run id with cumulative counters preserved; a proposal past the repair quota is
refused and the node fails. **W9** — an oversized request parks with a reason naming the
dimension, the amount and the capacity, in **both** loops. **W10** — editing an
awaiting-verification job's ROADMAP `resources:` block mid-run leaves its recorded hold
unchanged. **W11** — `maestro workflow runs <task>` lists both runs of a repaired task in
start order, with `--json`.

Then the P12A close-out and, after that, P12.

## When this session ends

Measure the context budget rather than estimating it:

```bash
python3 ~/.claude/skills/close-session/scripts/context_usage.py
```

The thresholds are **absolute token counts, not percentages** — the window is ~1M, so a
session can read as a comfortable 21% and still be well past the point where a reset pays
for itself. For Claude Opus 5: prepare a handoff at 160K, start a fresh session by 200K,
exception ceiling 300K. S2 ended at 214.6K; the S3 step-A session handed off at ~163K. The orchestrating
session's own context grows ~5-10K per step, so B -> C -> D plus close-out should fit;
if it crosses 160K before the close-out, hand off again.

**Write the next handoff as a fully self-contained file in `handoffs/`.** Dan starts a
session with nothing but `continue from handoffs/<file>.md` — no other prompt follows, so
anything you leave only in the chat is lost. Put every instruction, baseline number,
commit hash, coordination quirk and hard-won trap in the file itself, open it by saying it
is the complete brief, and keep the closing chat message to the outcome, the verified
numbers and that one `continue from …` line. Follow the format of this file and of
`handoffs/2026-09-15-graph-p12a-s2b.md`.
