# Handoff — P12B S1b: F2 (agent-CLI failure classification) and F3 (durable failure record)

**This file is the complete brief.** Dan starts the session with nothing but
`continue from handoffs/2026-09-17-graph-p12b-s1b.md`. No other prompt follows and there is
no chat context. Read this file in full, then follow the read order.

Working directory: `/home/dan/projects/maestro`. Branch `feat/graph-engineering-foundation`
(**no upstream**, so `git pull` fails; that is expected).

---

## Where things stand

- **P12B S1a is done** (2026-09-17). Commits:
  - `2b572de` F0: `maestro/workflows/failures.py` (`FAILURE_KINDS`, `FailureRecord`, `classify_node_result`, `classify_exit`, `record_from_runtime`) and `tests/workflows/test_failures.py`.
  - `761bacc` F1: `model_api` typed `TransportError` subclasses (`RateLimited`, `AuthFailed`, `ProviderUnavailable`, `RequestTimeout`, `ContextOverflow`), `classify_http_failure`, `parse_retry_headers`, and `ToolCall.malformed`. `ModelAgentRuntime` now sets `stop_reason` to the failure kind, detects `empty_output` and `malformed_tool_calls`, and refuses a malformed call as a tool result. `worker._run_model_endpoint` writes `outputs["failure"]`. Tests are in `tests/backends/test_model_api.py` and the tail of `tests/backends/test_model_runtime.py`.
  - The full suite after F1: junit tests=4629 failures=0 skipped=1, exit 0.
- **STATE** (`docs/graph-engineering/STATE.yaml`, git-ignored): `current_phase: P12B`, `status: in_progress`, and `in_progress_details` filled in. The 3 `_p12b_open_threads` (owner P12B) are still open.
- **Still to do in S1b:** F2 and F3. After that come S2 (F4, F5) and S3 (F6, F7, F8, then the close-out).

## Read order

1. **`handoffs/2026-09-17-graph-p12b-s1-design.md`**: the binding design contract for F0–F3. Its F2, F3 and Tests sections are your spec. F0/F1 there are already built; read them only for the names you call.
2. `docs/graph-engineering/specs/phases/P12B_MODEL_FAILURE_HANDLING.md`: §1, F2, F3, §3 (F2/F3 acceptance) and §4.
3. `handoffs/2026-09-17-graph-p12b-implementation.md`: the phase brief, with Dan's decisions, invariants, coordination and the S2/S3 split. It still applies. At the end of S1b, write the S2 handoff in the same style.
4. Memories: `llm-failure-handling-policy`, `harness-auto-updates-must-not-break-gates`, `context-management-not-built-independent`.

## Code map (already surveyed, to save you the reads)

- `maestro/workflows/worker.py`:
  - `_run_agent_cli(args, context, brief)` currently throws away `driver.complete(...)`'s `Completion`. The driver is `registry.get_backend(registry.driver_name_for_backend_id(args.backend_id))`, and the log is `context.inbox.path / "impl.log"`.
  - In `run()`, the `_run_agent_cli(...)` call is followed by `handlers.resolve(node.handler_ref)(context)` (the adoption).
- `maestro/backends/base.py`:
  - `Completion(text, returncode, timed_out)`;
  - `ExitVerdict(kind: ok|quota_exhausted|crashed, reset_at, evidence)`;
  - `driver.parse_exit(rc, log_tail)`, implemented by claude, codex and antigravity.
- `maestro/workflows/runner.py`:
  - `_commit_result` (around line 644) decides the phase and calls `lifecycle.finish_attempt` and `set_node_phase`, then `_record_settle_telemetry` (under routing only);
  - add the failure write next to that, only when `self.routing is not None` and the phase is `failed`;
  - `node.handler_ref` gives the handler.
- `maestro/control/store.py`: the `attempts` DDL is around line 285, and `_ADDED_COLUMNS` is around line 398 (add the 3 nullable TEXT columns to both).
- `maestro/control/lifecycle.py`: `_set_attempt_fields` (around line 397) has an `allowed` set. Add the 3 columns and a `record_failure` method.
- `maestro/status.py`:
  - `workflow_view` (around line 438) already loads every `attempts` column;
  - `explain_view` (around line 629) builds per-attempt dicts, so add `"failure"` there;
  - `render_explain_text` renders it;
  - `list_runs`, `_task_runs` and `_open_store` are the patterns to copy for `list_failures`.
- `maestro/cli.py`:
  - `cmd_workflow` is around line 1444;
  - the parser is around line 1923 (`verb` choices `["show", "runs"]`, and `task_id` is positional, so make it `nargs="?"`);
  - dispatch is around line 1982.
- Tests:
  - `tests/graph_engineering/test_dispatch_wiring.py` has the helpers: `_graph_scenario`, `_windows`, `_worker_argv`, `_attempts`, `_attempt_rows`, `_node_phase`, the `resolutions` fixture and `FREE_ENDPOINT`;
  - it also has the worker-in-`on_poll` patterns: `_run_the_gate_worker` (around line 1255) and `_answer_with_a_proposal` (around line 1483).
  - Create `tests/graph_engineering/test_failure_ladder.py`, which may import those helpers from `.test_dispatch_wiring`.
  - To drive a fake CLI driver, monkeypatch `registry.get_backend` (or `registry.driver_name_for_backend_id`) inside the worker run, and spy on `handlers.resolve` to prove that adoption was not called.

## Invariants (unchanged, binding)

- S1 only classifies and records. A classified failure still writes `failed`, exactly as today. Nothing here changes what is dispatched next.
- Write the failure columns only when `routing is not None`. `compile_workflow` without escalation is untouched.
- One dispatch choke point. No Context Gate. Don't touch the `P12A_*` spec, `AGENTS.md` or `GEMINI.md`. Scratch files go in `.scratch/`.
- A PreToolUse hook rejects Bash text containing the privileged word (s-u-d-o).
- pytest's summary line is suppressed: read the counts off the junit root `testsuite`.
- Commit messages end with `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Make one commit for F2 and one for F3.
- Edit STATE with targeted replacements only. At the end, update `in_progress_details` (`what_was_done`/`whats_left`).

## Verification (report the numbers)

```bash
.venv/bin/python -m pytest -q tests/workflows/test_failures.py tests/graph_engineering/test_failure_ladder.py tests/graph_engineering/test_dispatch_wiring.py tests/backends tests/workflows tests/test_workflow_status.py --junit-xml=.scratch/p12b/gate.xml
.venv/bin/python -m pytest -q --junit-xml=.scratch/p12b/full.xml   # must exit 0
```

Report the gate and full-suite junit counts, the F2/F3 commit hashes, and the path of the S2 handoff you wrote.

For Opus 5, prepare a handoff at 160K tokens and start fresh by 200K. The next handoff must be a self-contained file in `handoffs/`. S2 needs its own design contract (`handoffs/…-p12b-s2-design.md`) before any code.
