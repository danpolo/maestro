# Handoff: Graph P12, session 3g. Watch run 5 of 02, continue the pilot, close P12

Continue Phase P12 on `feat/graph-engineering-foundation` (maestro). This file is the whole prompt. It
**supersedes** `handoffs/2026-09-25-graph-p12-s3f-run4-and-g7.md`. `docs/graph-engineering/STATE.yaml` and
`handoffs/` are gitignored (edits live on disk only).

## 0. Read first (and only these)
1. `handoffs/2026-09-18-graph-p12-s3-pilots.md` §1, §3 (steps 5, 7, 8, 9), §4, §5, §6.
2. `docs/graph-engineering/STATE.yaml`: search `Pilot finding D11`, `Pilot finding D12`, `D10 fix edges`,
   `parity batch G7`.
3. `docs/DESIGN.md` §16 "Parity with the legacy loop" (G7 now carried/replaced there).
4. `handoffs/2026-09-25-graph-g7-legacy-parity-open-items.md`: its status table and "design calls" (what the
   operator verbs mean now under the graph: manual-step Done/Redo/Abandon, `/unpark` reopen, Retry/Shelve).
5. `artifacts/graph-engineering/p12-pilots/manifest.json` `.amendments` (6 entries).
6. Memory `risky-files-gated-by-stronger-reviewer.md`.

## 1. What session 3f did (2026-09-26)
- **D12 `ad742b7`** (Dan received no Telegram message from the test bot): the watchdog starts `maestro run` with
  `tmux new-window`, which inherits the tmux *server's* env, so `.env` never reached the loop and every notice was
  skipped silently while journaled as sent. `cmd_run`/`cmd_watchdog` now load `<repo>/.env` (override=True); the
  `roadmap_map_pushed` / `graph_run_failed_notified` journal lines append ` telegram=unconfigured` when nothing
  could be sent. The bot `@MaestroGenericTestingBot` was verified live (one test sendMessage, HTTP 200). **Not yet
  confirmed with Dan: that he got run 5's start notice.** Ask him.
- **D11 `2b80705`**: claude "hit your monthly spend limit … resets 2pm (Tz)" → `rate_limited` with the reset;
  codex "You’ve hit your usage limit … try again at [date] H:MM AM|PM" → `quota_exhausted` with the reset in local
  time. Both are `blocked` (pool pause, no retry spent). Tests use run 4's exact strings.
- **G7 `be3b0c6..508a9a7`** (8 commits, one worktree agent): manual lane has `merge` (template 1.1.0), human /
  await_job / cooldown pause nodes are released, B14 prep contract in the prep brief, `/unpark` reopens a fresh run
  on the kept branch, graph meanings for `/approve /reject /redo`, `/fix` and `/backend` answer truthfully, failed
  notice is a Dan request with Retry/Shelve. Left open: G7 item 7 (idle self-update during pilots) and `/ask` only
  seeing legacy `waiting_on_dan`.
- Full suite at 508a9a7: junit tests=4883 failures=2 errors=0 skipped=1. Both failures are `test_purity` on the
  untracked `docs/ABUALI_INTEGRATION_BOUNDARIES.md` (not ours; leave it, ask Dan if it gets in the way) → 0 real.
- Run 4 archived: `duetflow/.orchestrator/archive/2026-09-25-run4` (DB+WAL/shm, attempts, artifacts, HALT), branch
  `archive/02-run4`, export `p12-pilots/duetflow-run4-archived.json` (9 attempts, rework 2).
- Adopted `508a9a7` into DuetFlow (doctor `adopted_code` OK + the two known WARNs). Amendment + export `81d864b`.
- Both agent worktrees and their branches are removed.
- **Run 5 of 02** (`run_hEXMbgJBtrZZZCTk`) opened 2026-09-26T00:24:52Z. Launched with
  `setsid nohup bash launch.sh` after creating the `agents` tmux session (it did not exist; `maestro run` refuses
  without it). Journal line count before the launch was 71.

## 2. In scope, in order
1. **Run 5:** wait with a background `until` loop on the control DB
   `/home/dan/projects/duetflow/.orchestrator/control.sqlite3` (`task_runs.status`, attempts), never repeated sleeps.
   Only match journal lines after a baseline (`base=$(wc -l < journal.ndjson)`, then `tail -n +$((base+1))`). When it
   settles, check: the proof_review verdict and rework count; a reviewer of a risky diff (`spotify.py`) on a higher
   strength rung than the writer; no `stall_restart` during long attempts (D9); Telegram journal lines **without**
   `telegram=unconfigured` (D12); if a quota limit hits, a pool pause (blocked, reset parsed) instead of burnt
   retries (D11). After an accept: checkout clean and at main, no `stale_checkouts`, graduation touched only docs,
   `push_skipped_no_remote` once.
2. Continue the pilot per s3 §3 and s3b §3 steps 7–8 (03's V-DAN: Dan picks his playlist on Telegram; 06 needs
   `duetflow collect`; 08-timers-notify is the manual lane, now with a merge). To rerun a task, archive its run first
   (DB with WAL/shm, attempts, artifacts; rename `impl/<task>` to `archive/<task>-runN`). Now `/unpark` also opens a
   fresh run on the kept branch — for a qualification rerun, still archive first so each run's record stays separate.
   Overwrite the untracked `p12-pilots/duetflow.json` only with the final export.
3. **Adopt only between pilot tasks, never under a live run**: `ctl halt`, adopt
   (`selfupdate.materialize_worktree` + `adopt(sha, project_repo=...)`, `MAESTRO_REPO=/home/dan/projects/duetflow`),
   append a manifest `code_under_test` amendment, relaunch. **Any maestro code commit while the loop runs can be
   adopted by the idle self-update (G7 item 7)** — halt DuetFlow before committing maestro code.
4. Then s3 §3 steps 5, 7, 8, 9: retention re-measure, qualification re-run, verdict, close P12. Name as next-task
   candidates: model-catalog-config thread; the failure-handling phase (D4, plus the D10 edge: strong writer → no
   stronger reviewer → park); the model-endpoint reviewer verdict gap; G7 item 7 (pin pilots / record adoptions);
   `/ask` under the graph.

## 3. Coordination and traps
- No other session writes maestro or DuetFlow. In the `agents` tmux session, kill only pilot windows; kill by PID.
- Auto mode refuses: writing to the journal, reading bot token values, `git worktree remove --force`,
  `git restore --worktree` + `revert` on DuetFlow main. Hand such steps to Dan as one `! <command>` line.
  (Using the token in a script that prints only HTTP status worked in s3f.)
- `ctl` is `/home/dan/projects/duetflow/.venv/bin/maestro ctl …` with `MAESTRO_REPO` set. `ctl halt` works while the
  loop runs; `pause`/`resume` refuse while it runs.
- pytest: `.venv/bin/python -m pytest --junitxml=<file in repo root>` (no -q). Delete stray `.junit-*.xml`.
- STATE.yaml threads must parse; quote any `owner:` containing `: `.
- An agent worktree under `.claude/worktrees/` must be unlocked (`git worktree unlock`) and its `.venv` /
  `docs/graph-engineering` symlinks removed before `git worktree remove` (no --force).
- Hand off again at ~120K context.

## 4. Report back (with numbers)
As in s3 §6, plus one line each with commit for D5 (3d4bd09), D6 (3898496), D7 (96570a0), D8 (9c61367), D9 (a86542b),
D10 (d0452f8/da16113), D11 (2b80705), D12 (ad742b7), and G1–G7 status (G7 be3b0c6..508a9a7; item 7 open). Per 02 run,
review verdict and rework count:
- run 1: hung on D4, after D9 stall kills;
- run 2: accepted despite 4 P1s (D6), reverted;
- run 3: 2 rejections, then parked on D10;
- run 4: 1 valid rejection, then parked in ~3 min on D11 (export: 9 attempts, rework 2);
- run 5: fill in.
