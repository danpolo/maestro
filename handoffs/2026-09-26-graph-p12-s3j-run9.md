# Handoff: Graph P12, session 3j. Adopt 3fd165c into DuetFlow, run 9 of 02, continue the pilot, close P12

Continue Phase P12 on `feat/graph-engineering-foundation` (maestro). This file is the whole prompt. It
**supersedes** `handoffs/2026-09-26-graph-p12-s3i-diagnoser-repair.md`. `docs/graph-engineering/STATE.yaml` and
`handoffs/` are gitignored (edits live on disk only).

## 0. Read first (and only these)
1. `handoffs/2026-09-18-graph-p12-s3-pilots.md` §1, §3 (steps 5, 7, 8, 9), §4, §5, §6.
2. `docs/graph-engineering/STATE.yaml`: search `Pilot finding D17`, `Pilot finding D16`.
3. `git show 3fd165c --stat` and its message (what changed and why).
4. Memories `llm-failure-handling-policy.md`, `gaps-are-not-decisions.md`.

## 1. What session 3i did (2026-09-26)
- **maestro 3fd165c** (TDD, lanes in `tests/graph_engineering/test_end_to_end.py`):
  - D17: diagnoser brief names `plan_repair.json` (verdicts poor_agent / bad_graph) plus a REPAIR CONTRACT
    section (failing node, "the writer whose work it judged", current graph JSON). `handlers._adopt_plan_repair`
    adopts it into `outputs.plan_repair` (non-JSON carried raw → refused by `from_dict`). Compact form
    `{"verdict":"poor_agent","reason":..,"brief_addenda":{"implementer":"..."}}` is expanded by
    `runner._completed_diagnosis` (workflow_id/base_revision default to the run's; addenda → replace_nodes with
    `params.brief_addendum`). `brief_addendum` is now rendered into the node's brief ("DIAGNOSIS OF THE EARLIER
    ROUNDS ... final run"); it was never rendered before. `_verdict_refusal`: poor_agent may re-brief the failing
    node or the writer it judged (`runner._blamable`) — in 02 the failing node is `proof_review`.
  - Found by the lane and fixed: after a repair, the invalidated `gate` was refused forever ("retry policy permits
    1 attempt(s); 1 already made", carried attempt_index) → the repaired run would hang. `pre_validate` now budgets
    any node on the retry ladder (`retry_cone` non-empty) by this run's resets, like agents.
  - `review_history` spans all runs of the workflow, so the final run's reviewer sees earlier rounds.
  - Item 2 (each review round aims to be the last): reviewer header + `reviewer.yaml` ask for rule / scope /
    verify per blocking finding, all in one pass; optional `rule`/`scope`/`verify` fields are appended to the
    finding string (`handlers._split_findings`); `runner.rework_note` tells the implementer to fix the class, add
    a general test per finding, check adjacent behaviour. `diagnoser.yaml` updated.
  - Full suite: 4892 tests, 2 failures (both `test_purity` on untracked `docs/ABUALI_INTEGRATION_BOUNDARIES.md`,
    not ours) → 0 real.
- NOT done: adoption into DuetFlow, manifest amendment, run 9. DuetFlow is still **HALTED** on maestro 0e80da0.
- Semantics to watch live: the rework budget is QUALITY_ATTEMPTS=3 → 3 review rounds → diagnose (billing-only,
  stronger model) → one final run (implementer with addendum, gate, proof_review) → accept, or park (no second
  diagnosis; `repair_budget_exhausted`).

## 2. Dan's decisions (do not re-ask)
- Retry-After: cap below the hourly interval, fail fast beyond (in PLAN §5 now).
- Review bar: blocking vs follow_up, reviewer sees prior rounds; each round aims to be the last (done 3fd165c).
- Failure policy (memory): 2 retries → stronger diagnoser → replan or enhance prompt → one final run → park.

## 3. In scope, in order
1. **Adopt 3fd165c into DuetFlow** (it is halted): `selfupdate.materialize_worktree` + `adopt(sha,
   project_repo=...)`, `MAESTRO_REPO=/home/dan/projects/duetflow`; doctor (expect OK + 2 known WARNs). Manifest
   `code_under_test` 0e80da0→3fd165c amendment with a `note` (use `json.dumps(..., indent=2, ensure_ascii=False)`
   — plain dump re-escapes existing criteria). Commit the maestro-side manifest change.
2. **Run 9 of 02**: remove HALT, `setsid nohup bash launch.sh` in DuetFlow (the `agents` tmux session must
   exist). Watch with a background until-loop on the control DB (`task_runs.status` of the newest run not
   `running`, or any `uncertain` attempt, or HALT, or a `park` node `claimed`). Check: verdicts, findings carry
   rule/scope/verify, follow-ups, rework count, the diagnose attempt `succeeded` with `plan_repair.json` in its
   workspace, `graph_run_repaired` journaled, revision 2's implementer brief has the DIAGNOSIS section, revision 2's
   gate runs, final review sees PRIOR REVIEW ROUNDS. After an accept: checkout clean at main, no
   `stale_checkouts`, graduation touched only docs, `push_skipped_no_remote` once. If it parks, archive (below)
   and diagnose from evidence before any fix.
3. Continue the pilot per s3 §3 and s3b §3 steps 7–8 (03's V-DAN: Dan picks his playlist on Telegram; 06 needs
   `duetflow collect`; 08-timers-notify is the manual lane with merge). Archive before any rerun (export first,
   `git branch -m impl/<task> archive/<task>-runN`, move control.sqlite3*, attempts, artifacts, HALT copy into
   `.orchestrator/archive/<date>-runN`, keep a HALT in place). Overwrite the untracked `p12-pilots/duetflow.json`
   only with the final export.
4. Then s3 §3 steps 5, 7, 8, 9: retention re-measure, qualification re-run, verdict, close P12. ITV only if Dan
   reports grill-me done. Next-task candidates: model-catalog-config; the failure-handling phase (D4 uncertain
   nodes hang forever; D10 edge); model-endpoint reviewer verdict gap; G7 item 7 (idle self-update during
   pilots); `/ask` under the graph.

## 4. Coordination and traps
- No other session writes maestro or DuetFlow. In the `agents` tmux session, kill only pilot windows; by PID.
- Halt DuetFlow before committing maestro code (idle self-update, G7 item 7). Adopt only between pilot tasks.
- Auto mode refuses: writing to the journal, reading bot token values, `git worktree remove --force`,
  `git restore --worktree` + `revert` on DuetFlow main, and having codex write outside its grant (negative
  sandbox probes). Hand such steps to Dan as one `! <command>` line.
- `ctl` is `/home/dan/projects/duetflow/.venv/bin/maestro ctl …` with `MAESTRO_REPO` set.
- pytest: `.venv/bin/python -m pytest --junitxml=<file in repo root> -n auto` (no -q). Delete stray `.junit-*.xml`.
- STATE.yaml threads must parse; quote any `owner:` containing `: `.
- A 02 run takes ~25 min to park (3 rounds + diagnose). Foreground `sleep` is blocked; use a background loop.
- Hand off again at ~120K context.

## 5. Report back (with numbers)
As in s3 §6, plus one line each with commit for D5 (3d4bd09), D6 (3898496), D7 (96570a0), D8 (9c61367), D9 (a86542b),
D10 (d0452f8/da16113), D11 (2b80705, live run 7), D12 (ad742b7, confirmed by Dan), D13 (dd9a777, 9d91b4c, narrowed
0e80da0, live run 8), D14 (0e80da0, live run 8), D15 (0e80da0, live run 8), D16 (0e80da0; severity used live, no
follow-ups yet), D17 (3fd165c; live in run 9?), and G1–G7 status (G7 be3b0c6..508a9a7; item 7 open). Per 02 run, verdicts and rework:
- run 1: hung on D4, after D9 stall kills;
- run 2: accepted despite 4 P1s (D6), reverted;
- run 3: 2 rejections, then parked on D10;
- run 4: 1 valid rejection, then parked in ~3 min on D11 (9 attempts, rework 2);
- run 5: 2 valid rejections, codex rework could not commit → uncertain, hung (D13) (7 attempts, rework 2);
- run 6: same as run 5 (first D13 fix incomplete) (7 attempts, rework 2);
- run 7: 3 rejections (sol, sol, opus after codex quota pause), diagnose → parked; found D14–D16 (12 attempts, rework 2);
- run 8: 3 rejections, findings 4 → 3 → 1 converging, diagnose had no plan_repair → parked; found D17 (11 attempts, rework 2);
- run 9: fill in.
